このアプリケーションは、postgreデータベースにアクセスすることで動作します。

まず初めに、テーブルの作成とテストデータ挿入のため、このReadMe.txtと同じ階層にあるTestData.sqlを実行して下さい。

その後、アプリケーションがデータベースにアクセスできるようにするため、このReadMe.txtと同じ階層にあるtest.xmlの、IDタグとパスワードタグの部分にそれぞれpostgresのIDとパスワードを、データベース名タグの部分にデータベース名を入力して下さい。